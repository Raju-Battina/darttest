# Project guidance

- This is an ASP.NET Core MVC application targeting .NET 10.
- Keep controllers focused on request handling and move reusable business logic into services.
- Keep Razor views presentation-focused and use strongly typed view models for page data.
- Run `dotnet build` after changes and add focused tests for non-trivial behavior.
